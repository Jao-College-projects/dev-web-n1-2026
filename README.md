# N1 - Trabalho (Front e Back Separados)

Estrutura inicial do projeto organizada em dois blocos:

- `back`: API em TypeScript para simular regras do dominio imobiliario.
- `front`: aplicacao React + TypeScript com Bootstrap via CDN.

Este ponto inicial foi preparado para atender aos requisitos de organizacao, componentizacao e tipagem.
